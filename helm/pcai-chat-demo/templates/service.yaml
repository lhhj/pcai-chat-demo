apiVersion: v1
kind: Service
metadata:
  name: {{ include "pcai-chat-demo.fullname" . }}
  labels:
    {{- include "pcai-chat-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pcai-chat-demo.selectorLabels" . | nindent 4 }}
